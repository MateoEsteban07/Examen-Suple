package BusinessLogic.Interfaces;

public interface gmIDisparar {
    // Solo la firma del metodo. 
    // Recibe el tipo de municion (String) leido del archivo.
    void gmDisparar(String gmTipoMunicion);
}